<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login to your Account</title>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  
  <link rel="stylesheet" href="/styles/register.css" />
</head>
<body>
  <main class="page">
    <section class="card" role="form" aria-labelledby="loginTitle">
      <h2 id="loginTitle" class="card-title">Welcome Back!</h2>
      <p class="card-subtitle">Sign in to access your account</p>

      <form id="loginForm" action="/auth/login" method="POST" novalidate>
        
        <div class="field">
          <input id="email" name="email_username" type="text" required autocomplete="text" placeholder="john.doe@example.com" />
          <label for="email_username">Email or Username</label>
        </div>

        <div class="field password">
          <input id="password" name="password" type="password" required autocomplete="current-password" placeholder="••••••••" />
          <label for="password">Password</label>
          <button class="toggle" type="button" aria-label="Toggle password visibility"></button>
        </div>

        <button class="btn primary" type="submit">Sign In</button>

        <p class="meta">
          Need an account? 
          <a class="link" href="/auth/register">Sign up</a>
        </p>
      </form>
    </section>
  </main>
<script src="/scripts/login.js"></script>
  </body>
</html>